---
title: 'SDK Initialization'
---

#### Add the SDK Dependency

Add the following dependency to your app-level `build.gradle` file

<CodeGroup>

```kotlin Kotlin
implementation("com.twinalyze:analytics:1+")
```

```java Java
implementation("com.twinalyze:analytics:1+");
```

</CodeGroup>

In your **project-level** `build.gradle` make sure the following plugins are configured:

<CodeGroup>

```kotlin Kotlin
plugins {
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.10" apply false
}
```

```java Java
plugins {
    id 'com.android.application' version '8.2.0' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.10' apply false
}
```

</CodeGroup>

#### Initializing Analytics SDK in Android

Initialize the **SDK** in your `Application` class to start tracking sessions, events, and insights.

<CodeGroup>

```kotlin Kotlin
class MyApp : Application() {
    override fun onCreate() {
        super.onCreate()
        Analytics.getInstance().initialize(
            this,
            "YOUR_API_KEY",
            "YOUR_ORG_ID”,
            true
        )
    }
}
```

```java Java
class MyApp extends Application {
    override fun onCreate() {
        super.onCreate()
        Analytics.getInstance().initialize(
            this,
            "YOUR_API_KEY",
            "YOUR_ORG_ID”,
            true
        );
    }
}
```

</CodeGroup>

<Accordion title="Parameter Reference" defaultOpen="true">

<table class="sdk-table">
  <thead>
    <tr>
      <th>Parameter</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>`application`</td>
      <td>Android application context</td>
    </tr>
    <tr>
      <td>`apiKey`</td>
      <td>Your Project API key</td>
    </tr>
    <tr>
      <td>`orgId`</td>
      <td>Your organization ID (e.g. `twinalyze_demo_7356297484`)</td>
    </tr>
    <tr>
      <td>`debug`</td>
      <td>If debug true than take a screenshot</td>
    </tr>
  </tbody>
</table>
 
</Accordion>

<Card class="pass-card" title="Demo Account" icon="key">
  Email: info@twinalyze.com <br/>
  Password: 123
</Card>

<div class="download-app">
 <a href="https://github.com/twinalyze/twinalyze-android-food-delivery-app-demo.git" target="_blank">
    <img src="https://cdn.jsdelivr.net/gh/devicons/devicon/icons/github/github-original.svg" />
    <span class="app-name">Github Demo</span>
  </a>

  <span class="app-devider">|</span>

  <a href="https://drive.google.com/drive/folders/1czFBkIDrxdkPcQRpOcrwFnjzPujvh7K7" target="_blank">
    <img src="https://upload.wikimedia.org/wikipedia/commons/d/d7/Android_robot.svg"  />
    <span class="app-name">Download APK</span>
  </a>

  <span class="app-devider">|</span>

  <a href="https://app.twinalyze.com/" target="_blank">
    <img src="/s.svg"/>
    <span class="app-name">Demo</span>
  </a>
</div>




>✅ Best Practice <br/>
Always call `initialize()` inside your `Application` class — not inside an Activity — to ensure complete session tracking.

<CodeGroup>

```kotlin Kotlin
class MyApp : Application() {
    override fun onCreate() {
        super.onCreate()
        Analytics.initialize(
            this,
            "kh2RwGFsqNr1gh1NWWExAewbvIzYyNk",
            "twinalyze_demo_7356297484",
            true
        )
    }
}
```

```java Java
class MyApp extends Application {
    override fun onCreate() {
        super.onCreate()
        Analytics.Companion.initialize(
            this,
            "kh2RwGFsqNr1gh1NWWExAewbvIzYyNk",
            "twinalyze_demo_7356297484",
            true
        );
    }
}
```

</CodeGroup>